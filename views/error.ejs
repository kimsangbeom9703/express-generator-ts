<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        .error-container {
            background-color: #fff;
            border-radius: 8px;
            padding: 40px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 100%;
        }

        h1 {
            font-size: 2.5em;
            color: #e74c3c;
            margin-bottom: 20px;
        }

        h2 {
            font-size: 1.5em;
            color: #f39c12;
            margin-bottom: 20px;
        }

        pre {
            background-color: #2c3e50;
            color: #ecf0f1;
            padding: 20px;
            border-radius: 5px;
            font-size: 1em;
            text-align: left;
            white-space: pre-wrap;
            word-wrap: break-word;
            margin-top: 30px;
            max-height: 300px;
            overflow-y: auto;
        }

        .message {
            font-size: 1.2em;
            color: #333;
            margin-bottom: 15px;
        }

        .back-link {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            background-color: #3498db;
            color: #fff;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1.1em;
        }

        .back-link:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
<div class="error-container">
    <h1><%= message %></h1>
    <h2>Status: <%= error.status %></h2>
    <div class="message">
        <p>Oops! Something went wrong. Here's the error details:</p>
    </div>
    <pre><%= error.stack %></pre>
    <a href="/" class="back-link">Go back to Home</a>
</div>
</body>
</html>
